#!/usr/bin/env node
// const argv = require('yargs').argv
// const compiler = require('../lib/javascript');
// const fs = require('fs');

// let swaggerData;
// if (typeof argv.swagger === 'string') {
//     swaggerData = JSON.parse(fs.readFileSync(argv.swagger));
// }
// compiler({
//     swagger: swaggerData,
//     url: argv.url,
//     targetFolder: argv.targetFolder,
//     difinitionFolder: argv.difinitionFolder,
//     httpBase: argv.httpBase,
// });
